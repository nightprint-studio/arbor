{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "corvus",
  "description": "Capability for the Corvus (Git) product shell — its dedicated window, and the tabbed container that can host it as a tab. Listing `workspace` here rather than repeating these permissions in a capability of its own keeps the container's grants in step with the product's.",
  "windows": ["corvus", "corvus-*", "workspace"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-minimize",
    "core:window:allow-maximize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-unmaximize",
    "core:window:allow-start-dragging",
    "core:window:allow-is-maximized",
    "opener:default",
    "notification:default",
    {
      "identifier": "opener:allow-open-path",
      "allow": [{ "path": "**" }]
    }
  ]
}
