[package]
name         = "arbor-issue-tracker-api"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
authors.workspace      = true
description  = "Issue tracker trait + DTOs (Issue, Comment, IssueState, transitions) + runtime registry. Implementations (Jira, Linear, GitHub Issues, GitLab Issues) live in sibling crates."

[dependencies]
arbor-core = { path = "../../core" }

serde       = { workspace = true }
serde_json  = { workspace = true }
thiserror   = { workspace = true }
async-trait = { workspace = true }
