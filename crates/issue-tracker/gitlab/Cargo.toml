[package]
name         = "arbor-issue-tracker-gitlab"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
authors.workspace      = true
description  = "GitLab Issues implementation of IssueTracker. Re-uses GitlabClient (HTTP + auth + parsers) from arbor-git-provider-gitlab."

[dependencies]
arbor-core                = { path = "../../core" }
arbor-issue-tracker-api   = { path = "../api" }
arbor-git-provider-gitlab = { path = "../../git-provider/gitlab" }

serde       = { workspace = true }
serde_json  = { workspace = true }
thiserror   = { workspace = true }
tracing     = { workspace = true }
tokio       = { workspace = true }
async-trait = { workspace = true }

chrono = { version = "0.4", features = ["serde"] }
