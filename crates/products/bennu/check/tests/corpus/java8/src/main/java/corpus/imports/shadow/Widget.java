package corpus.imports.shadow;

/** A top-level type named like the nested {@code Widget}s the import cases import on demand. Must stay error-free. */
public class Widget {
    public void local() {
    }
}
