[package]
name         = "bennu-dtolab"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
authors.workspace      = true
description  = "The DTO Lab's pure half: the invalid values each constraint implies, the context validation-test templates are rendered with and checked on the JVM, a JSON skeleton — and the Java harness that answers the same questions for real, on the project's own JVM."

[dependencies]
bennu-templates = { path = "../templates" }
serde       = { workspace = true }
serde_json  = { workspace = true }
base64      = { workspace = true }
schemars    = { workspace = true }
