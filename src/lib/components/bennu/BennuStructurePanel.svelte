<script lang="ts">
  /** Structure (left tool window) — the active file's Java symbols. Thin wrapper
   *  over the shared BennuSymbolList so Structure and Outline stay identical. */
  import BennuSymbolList from './BennuSymbolList.svelte';
</script>

<BennuSymbolList title="Structure" />
